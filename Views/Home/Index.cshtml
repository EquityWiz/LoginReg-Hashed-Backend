@{
    ViewData["Title"] = "Home Page";
    @model User
}
<div class="container row flex justify-content-center">
    <div class="card p-3 bg-info text-white shadow-sm mb-5 rounded col-4">
        <partial name="RegPartial"/>
        <a href="/login" class="link-primary text-center">Already a user?</a>
    </div>
</div>
